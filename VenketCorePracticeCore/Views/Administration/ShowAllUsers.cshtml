@model List<RegisterUsers>

@{
    ViewBag.Title = "Users List";

}

@foreach (var item in Model)
{
    <div class="card">
        <div class="card-header">
            User ID - @item.ID
        </div>
        <div class="card-body">
            <h5 class="text-center bg-info">@item.UserNam</h5>
        </div>
        <div class="card-footer">
            @*<form method="post" asp-action="DeleteUsers" asp-route-id="@item.ID">

                    <a asp-action="editUser" asp-controller="Administration" asp-route-id="@item.ID" class="btn btn-primary">Edit</a>

                    <button class="btn btn-danger" type="submit" onsubmit="return confirm('Are you sure you want to delete this ? : @item.UserNam')">Dalete</button>


                </form>*@


            <a asp-action="editUser" asp-controller="Administration" asp-route-id="@item.ID" class="btn btn-primary">Edit</a>
            <form method="post" asp-action="DeleteUsers" asp-route-id="@item.ID">

                <span style="display:none" id="confirmDeletespan_@item.ID">

                    Are you Sure you want to delete ?
                    <button class="btn btn-danger" type="submit">Yes</button>
                    <a href="#" class="btn btn-primary" onclick="DeleteConfirm('@item.ID', false);">No</a>
                </span>
            </form>

            <span id="deletespan_@item.ID">
                <a href="#" class="btn btn-danger" onclick="DeleteConfirm('@item.ID', true);">Delete</a>
            </span>


        </div>
        
    </div>
}



@section scripts{
    @*<script src="~/lib/jqueryfiles/jquery.js"></script>*@
    <script src="~/scripts/CustomScript.js"></script>

    @*<script type="text/javascript">
        function DeleteConfirm(uniqueID, isDeleted) {
            var deletespan = 'deletespan_' + uniqueID;

            var confirmDeleteSpan = 'confirmDeletespan_' + uniqueID;

            if (isDeleted) {
                $("#" + deletespan).hide();
                $("#" + confirmDeleteSpan).show();
            }
            else {
                $("#" + deletespan).show();
                $("#" + confirmDeleteSpan).hide();
            }
        }
    </script>*@

}
